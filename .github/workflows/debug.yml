name: Debug CI
on:
  push:
    branches:
      - 'branch-*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: |
          git show --format=fuller HEAD
